import json
from structure import Graph, Node



# def parse_graphData(graph_json):
#     # Parse the JSON object
#     node_dict = json.loads(graph_json)
#     return_list = []
#     for node in node_dict.json().nodes
#         # Convert the object into the desired format
#         formatted_data = {
#             'id': str(node['id']),
#             'position': {
#                 'x': node['x_coord'],
#                 'y': node['y_coord']
#             },
#             'data': {
#                 'label': str(node['id'])
#             }
#         }
#         return_list.append(node)

#     return return_list

# def getEdges(graph_json)

